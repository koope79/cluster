#JSGF V1.0;

grammar cmdGrammar;

public <basicCmd> = <cmdTimer>;
<cmdTimer> = <actionTimer> <timer> <pretext> <parameterTimer> <time>;
<timer> = (таймер);
<actionTimer> = (включи|поставь);
<parameterTimer> = (одну|пять|десять);
<time> = (минут|минуту);
<pretext> = (на);
